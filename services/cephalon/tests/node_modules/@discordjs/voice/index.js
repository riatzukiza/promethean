const { EventEmitter } = require('events');
let lastJoinOptions = null;
function joinVoiceChannel(options) {
  lastJoinOptions = options;
  return { receiver: { speaking: new EventEmitter() }, destroy() {} };
}
function getVoiceConnection() { return null; }
function createAudioPlayer() { return { on() {} }; }
function createAudioResource() {}
const AudioPlayerStatus = { Idle: 'idle' };
const EndBehaviorType = { AfterSilence: 0 };
const StreamType = { Raw: 'raw' };
module.exports = { joinVoiceChannel, getVoiceConnection, createAudioPlayer, createAudioResource, AudioPlayerStatus, EndBehaviorType, StreamType, lastJoinOptions };
